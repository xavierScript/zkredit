import type { Metadata } from "next";
import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";
import { WalletContextProvider } from "./src/contexts/WalletContextProvider";
import { NotificationProvider } from "./src/contexts/NotificationContext";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata: Metadata = {
  title: "ZKredit | Private Lending Protocol",
  description: "The first privacy-preserving lending protocol on Solana. Borrow and lend with confidence.",
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en">
      <body
        className={`${geistSans.variable} ${geistMono.variable} antialiased`}
      >
        <WalletContextProvider>
          <NotificationProvider>
            {children}
          </NotificationProvider>
        </WalletContextProvider>
      </body>
    </html>
  );
}
